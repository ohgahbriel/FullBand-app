# FullBand backend dependencies.
# IMPORTANT: install into a Python 3.11 or 3.12 virtualenv.
# PyTorch / Demucs do not yet publish wheels for Python 3.14.
#
# Install PyTorch first, matching your CUDA toolkit, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
yt-dlp>=2024.12.13
demucs==4.0.1
python-multipart==0.0.20

# BPM + musical-key detection and click-track synthesis.
librosa>=0.10.2
soundfile>=0.12.1
